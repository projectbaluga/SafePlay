name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: msbuild PoringProtect.sln /t:Build /p:Configuration=Release /p:Platform=x64

